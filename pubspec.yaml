name: gd6_b_1815
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  sqflite: ^2.2.6
  flutter_slidable : ^3.1.1

flutter:
  uses-material-design: true
